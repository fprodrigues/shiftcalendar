<%= image_tag('peracalendar.png', :id =>'logo')%>
<p id="notice"><%= notice %></p>

<h3 id='texto_index'>Veja se a sua já está abaixo, senão, crie; se estiver errada, destrua e faça novamente.</h3>

<table id = 'escala_index'>
  <thead>
    <tr>
      <th>Escala</th>
      <th>Ano</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @escalas.each do |escala| %>
      <tr>
        <td><%= escala.nome %></td>
         <td><%= escala.ano %></td>
        <td><%= link_to 'Show', escala, :id =>"btn" %></td>
        <td><%= link_to 'Edit', edit_escala_path(escala), :id =>"btn" %></td>
        <td><%= link_to 'Destroy', escala, method: :delete, :id=>"btn", data: { confirm: 'Tem certeza? (Are you sure?)' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Escala', new_escala_path,:id => 'btn_e' %>
